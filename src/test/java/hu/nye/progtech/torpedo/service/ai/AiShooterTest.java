package hu.nye.progtech.torpedo.service.ai;

import static org.mockito.Mockito.verify;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.List;

import hu.nye.progtech.torpedo.model.GameState;
import hu.nye.progtech.torpedo.model.TableVO;
import org.aspectj.lang.annotation.Before;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.*;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
public class AiShooterTest {
/*
    private static final List<Character> ROW = List.of(' ', 'o', 'X', ' ', ' ', ' ',  ' ',  ' ', 'o', 'X');

    private AiShooter underTest;




    @Test
    public void testShootShouldSetTileToXOrPlus() {
        // given
        ArrayList<List<Character>> playerList = new ArrayList<>();
        for (int i = 0; i < 10; i++) {
            playerList.add(ROW);
        }
        TableVO playerTable = new TableVO();
        playerTable.setTable(playerList);
        TableVO aiTable = Mockito.mock(TableVO.class);
        gameState = new GameState(playerTable, aiTable);

        underTest = new AiShooter(gameState);

        // when
        underTest.shoot();

        // then
        verify(gameState).getCurrentTable();

    }*/



}
